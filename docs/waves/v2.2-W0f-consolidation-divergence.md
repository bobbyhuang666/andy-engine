# v2.2-W0f 任务卡 — Consolidation Merge Decision Divergence Diagnosis

> 阶段：v2.2-W0f 诊断（非实现，**最后一轮 L4 根因诊断**）
> 触及边界：**否**——仅诊断、dump、instrument、写报告。
> 状态：总规划师已批准启动
> 依赖：W0e（presentations 截断根因，已修）+ W1 partial（4 层修复工作树改动保留未提交，作为诊断基线）

## 1. 背景

W1 修 4 层（_nextId + counters + toJSON presentations + 运行时 presentations）后，retrieve 选择在 tick 50-83 完全一致（10 次无差异），但 L4 仍 fail（tick 83 memLen 41 vs 53）。accessCount 差 3 来自 consolidate（line 725）合并累加——full 与 restored 的 consolidate 合并了不同 pair。

W0f 定位 consolidate 合并决策差异的精确根因。**最后一轮**，W0f 后不再开 W0g。

## 2. 边界（总规划师划定，全程有效）

**允许**：诊断脚本、临时 instrumentation、诊断报告。

**禁止**：
- 不提交当前 4 层修复。
- 不重生成 golden fixture。
- 不 bump schemaVersion / Stable Envelope / public API。
- 不做 speculative consolidate 重写。
- 只做诊断和报告。

## 3. 止损规则（总规划师定）

- 若定位到明确 persistence 缺口 → 纳入 W1 一并修复。
- 若定位为排序稳定性 / floating tie-breaker → 允许作 v2.2 determinism hardening 修复，须极小、可证明不改变设计语义。
- 若 W0f 后仍无法闭合 → 停止 L4 实现，接受当前 4 层修复作 v2.2 persistence hardening，L4 full pass 降级 v2.3，记录剩余根因。

## 4. W0f 必须回答的 8 个问题

1. 第一次不同的 consolidate tick 是多少？
2. 第一个不同的 merge pair 是什么？
3. pair 差异由哪个评分分量导致？
4. 该分量依赖的字段是否被 toJSON/fromJSON 持久化？
5. full/restored 的 memory array order 是否一致？
6. RNG draw 是否一致？
7. 是否是浮点/排序稳定性问题？
8. 修复属于 persistence fidelity，还是 simulation determinism hardening？

## 5. 诊断方法

instrument `PersonalMemory.consolidate` 内部：
- 每次 merge 的 keep/remove memory id
- similarity score
- importance / accessCount / presentations length
- createdAt / timestamp / lastAccessed
- emotionSnapshot
- semantic category / content hash
- sort / traversal order
- logisticNoise / RNG draw
- 合并 pair 选择前后的 memory array order

dump tick 60/72（consolidate 触发点，_ticksSinceReflection 达 12）+ tick 83 前后，对比 full vs restored。

## 6. 写入边界

| 文件 | 说明 |
|---|---|
| `scripts/l4-consolidation-divergence-diag.js` | 诊断脚本 |
| `docs/current/CONSOLIDATION_DIVERGENCE_ROOT_CAUSE_REPORT.md` | W0f 交付物 |

**不得改**：任何 src/ production code（W1 工作树改动保留不扩大）。

## 7. 不做的事

- 不改 production code / schemaVersion / Stable Envelope / public API / golden fixture。
- 不提交 W1 工作树改动。
- 不做 speculative consolidate 重写。
- 不开 W0g（最后一轮）。
