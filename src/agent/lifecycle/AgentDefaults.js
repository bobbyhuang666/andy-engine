/**
 * AgentDefaults — Default field values for Agent state.
 *
 * Single source of truth for initialization defaults.
 */

const AGENT_DEFAULTS = {
  socialEnergy: 0.7,
  health: 1.0,
  isOnline: true,
  reflectionInterval: 12,
};

module.exports = { AGENT_DEFAULTS };
