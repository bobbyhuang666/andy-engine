/**
 * config/ — Backward Compatibility Layer
 *
 * Re-exports from src/config/ for backward compatibility.
 * New code should import from 'andy-engine/config' or directly from src/config/.
 */

module.exports = require('../src/config');
